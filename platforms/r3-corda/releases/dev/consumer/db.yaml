apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: consumerdb
  annotations:
    fluxcd.io/automated: "false"
  namespace: consumer-ns
spec:
  releaseName: consumerdb
  interval: 1m
  chart:
    spec:
      chart: platforms/r3-corda/charts/h2
      sourceRef:
        kind: GitRepository
        name: flux-dev
        namespace: flux-dev
  values:
    replicaCount: 1
    nodeName: consumer
    metadata:
      namespace: consumer-ns
    image:
      containerName: ghcr.io/hyperledger/h2:2018
      imagePullSecret: regcred
    resources:
      limits: "512Mi"
      requests: "512Mi"
    storage:
      memory: 512Mi
      mountPath: "/opt/h2-data"
      name: azurestorageclass
    service:
      type: NodePort
      p2p:
        port: 10002
      rpc:
        port: 10003
      rpcadmin:
        port: 10005
      tcp:
        port: 9101
        targetPort: 1521
      web:
        targetPort: 81
        port: 8080
      annotations: {}
    deployment:
      annotations: {}
    pvc:
      annotations: {}
